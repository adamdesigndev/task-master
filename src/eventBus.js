// eventBus.js

// Import Vue's 'ref' to create a reactive reference
import { ref } from 'vue';

// Export a reactive reference to manage the state of the currently active menu
export const activeMenu = ref(null);
